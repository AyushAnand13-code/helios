# Full local development stack (dbt + MVP build + dashboard). Use this locally:
#   pip install -r requirements-dev.txt
# The lean requirements.txt (dashboard runtime only) is what Streamlit Cloud installs.
dbt-core>=1.7,<1.9
dbt-bigquery>=1.7,<1.9
google-cloud-bigquery>=3.17
scipy>=1.11
statsmodels>=0.14
prophet>=1.1
pmdarima>=2.0
pandas>=2.1
pyyaml>=6.0
mcp>=1.0
anthropic>=0.39
pytest>=8.0
streamlit>=1.30
