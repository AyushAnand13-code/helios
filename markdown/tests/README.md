# tests/

Cross-cutting / integration tests that span layers.

- dbt tests live in `dbt/` (schema + singular + custom-generic).
- MCP contract tests live in `mcp/`.
- The labeled benchmark (the real integration test) lives in `eval/`.

_Empty for now._
