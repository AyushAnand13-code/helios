packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
  # dbt_project_evaluator removed for the MVP: it's a CI-linting package whose own
  # models use DML (needs BigQuery paid tier) and add ~50 nodes of noise. Re-add at M10
  # (CI) once billing is on.
  - package: dbt-labs/codegen
    version: [">=0.12.0", "<0.13.0"]
